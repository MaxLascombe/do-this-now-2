# Blockers

Tasks the ralph loop could not complete and needs your input to unblock. Each entry names the task, describes what's needed, and was written by the agent at the time it got stuck.

Resolve the blocker, then re-run `./ralph.sh` (or `./ralph.sh --auto`). Once resolved, remove the entry from this file.

---

<!-- Blockers are appended below by the agent -->
